# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vite-hooks/_/
tmp/

# Rust diff-match-patch codec (bun run build:wasm). The .wasm artifact is
# committed (tiny, ~6.5 KB) so tests and Vercel use it without a Rust toolchain;
# the Cargo build directory is not.
src/core/dmp/target/
.vercel
